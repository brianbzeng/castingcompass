# CI exercises the smoke pipeline and its validation-identity contract. Keep the
# protocol lock as the constraint for overlapping numerical/cryptographic packages.
-r requirements-smoke.txt
-c requirements-validation.txt

# Preserve the reviewed pandas 3 behavior; 3.0.4 was yanked for reported segfaults.
pandas==3.0.5

# CI-only lint tool; not part of the research runtime identity.
ruff==0.15.22
